{{- if and .Values.rbac.create .Values.leaderElection.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "harness-gitops-agent-controller.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "harness-gitops-agent-controller.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "harness-gitops-agent-controller.fullname" . }}-leader-election
subjects:
  - kind: ServiceAccount
    name: {{ include "harness-gitops-agent-controller.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
